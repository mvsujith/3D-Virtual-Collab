services:
  - type: web
    name: sujith-collab-grid-plane-app
    env: node
    branch: main
    buildCommand: "npm install --no-audit --no-fund && npm run build"
    startCommand: "npm start"
    plan: free
    autoDeploy: true
